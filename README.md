# Business Leaders Association Website

Next.js + React + Tailwind site for the Business Leaders Association.

## Setup

```bash
npm install
npm run dev
```

Open `http://localhost:3000` for the site.

TinaCMS runs at `http://localhost:3000/admin/index.html`
