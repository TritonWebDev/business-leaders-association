import type { FaqContent } from "@/lib/types";

type FaqSectionProps = {
  content: FaqContent;
};

export function FaqSection({ content }: FaqSectionProps) {
  return <div>{/* TODO: Build the FAQ section */}</div>;
}
