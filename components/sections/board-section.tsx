import type { BoardContent } from "@/lib/types";

type BoardSectionProps = {
  content: BoardContent;
};

export function BoardSection({ content }: BoardSectionProps) {
  return <div>{/* TODO: Build the board section */}</div>;
}
