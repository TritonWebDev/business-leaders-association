import type { AboutContent } from "@/lib/types";

type AboutSectionProps = {
  content: AboutContent;
};

export function AboutSection({ content }: AboutSectionProps) {
  return <div>{/* TODO: Build the about section */}</div>;
}
