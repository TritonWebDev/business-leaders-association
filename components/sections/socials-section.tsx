import type { SocialsContent } from "@/lib/types";

type SocialsSectionProps = {
  content: SocialsContent;
};

export function SocialsSection({ content }: SocialsSectionProps) {
  return <div>{/* TODO: Build the socials section */}</div>;
}
